#include <iostream>
#include "mmr.h"
#include "myheader.h"
void main()
{
    // Creates a seed
    srand(static_cast<unsigned int>(time(0)));
    system("cls");
    
    // Calls the function to start
    start();
}
